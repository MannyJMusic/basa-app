---
layout: src/layouts/shopify.njk
title: Your payment couldn't be processed
preheader: Your payment couldn't be processed
---
{% extends "src/layouts/shopify.njk" %}

{% block shopifyhead %}
  {% raw %}
    {% capture title %} Hi Candy ⚠ {% endcapture %}

    {% capture body %} You added items to your shopping cart but the payment couldn't be processed. You haven't been charged. {% endcapture %}

    {% capture note %} You can still return to your cart to complete your purchase. Or <a href="{{ shop.url }}" class="text-accent hover-underline">visit our store</a>. {% endcapture %}

    {% capture button_label %} Return to cart {% endcapture %}
  {% endraw %}
{% endblock %}

{% block template %}

<table class="wrapper w-full all-font-sans">
  <tr>
    <td align="center" class="bg-white">
      <table class="w-640 sm-w-full">
        <tr>
          <td class="bg-white px-40 py-48 sm-px-16 sm-py-24 text-left">

            {% include "src/partials/shopify/header.njk" %}

            {% raw %}
            <table class="mb-24">
              <tr>
                <td class="bg-red-50 p-16 rounded">
                  <p class="text-12 leading-16 text-red-500 m-0">Your payment couldn't be processed</p>
                </td>
              </tr>
            </table>
            <p class="text-21 text-gray-900 m-0 leading-28">{{ title | strip }}</p>
            <p class="text-21 text-gray-900 m-0 mb-24 leading-28">{{ body | strip }}</p>
            <p class="text-gray-500 text-16 leading-22 m-0">{{ note | strip }}</p>
            <div class="leading-16 sm-h-16">&nbsp;</div>
            <table class="sm-w-full">
              <tr>
                <td align="center" class="bg-accent text-white rounded shadow" style="mso-padding-alt: 20px 32px;">
                  <a href="{{ url }}" class="inline-block sm-block text-white font-bold sm-text-14 text-16 leading-16 sm-py-16 py-20 px-32">{{ button_label | strip }}</a>
                </td>
              </tr>
            </table>
            <div class="leading-16 sm-h-16">&nbsp;</div>
            {% endraw %}

            {% include "src/partials/shopify/footer.njk" %}

          </td>
        </tr>
      </table>
    </td>
  </tr>
</table>

{% endblock %}
